快捷键
ctri + F  当前文件查询字符  F3下一个  shirft +F3上一个
ctri +Shirft+ F  所有文件查询字符
ctri +N     查找类
ctri +Shirft +N 查找文件
ctri+O 重写
ctri+shirft+alt+N 查找类中的方法或变量
ctri+alt + 回车 自动分号
alt+insert 实现 方法 复写方法 抽象类在 匿名内部类使用的时候 要打上{} 再用
ctri +Shirft + +/- 实现折叠打开关闭效果

字符串快速导入到resorce
点击字符串 alt+enter 


Toast 大写 可以快速生成代码

ctri+shirft+K push

翻译：ctri+shirft+Y
替换：ctri+shirft+X

ctri+P 方法参数提示
alt+Q 提示类名 方法名
alt+D 添加注释 自己设置的
ctri+R 替换
ctri+D 复制
ctri+Shirft+U 大小写变换


查看apk签名
1.解压apk-》zip 2 META-INF 中的rsa文件 用如下命令
keytool -printcert -file E:\code\android\Dialer\app\build\outputs\apk\debug\META-INF\CERT.RSA

查看界面任务战（主要看activity）
adb shell dumpsys activity activities

查看fragment
adb shell dumpsys activity +包名
更好的办法 adb shell dumpsys activity top  但是有pageview 的时候就有多个显示

查看versionCode
adb shell dumpsys package com.szqd.dialer | findstr versionCode
或者直接用E:\sdk\Android\build-tools\29.0.0 aapt 查看apk 
aapt dump badging "%filename%" //filename 为对应apk

多个手机都连着 怎么安装
adb devices 列出所有手机
adb -s LRQOMNVS99999999 install + "%filename%" //filename 为对应apk

提交的时候出错
git commit --amend -C HEAD --reset-author

git 用 https方式的时候对应的账户是网站的注册账户
    用 SSH 的时候 我理解是生成 ssh密钥时 的git 账户

全局
C:\Users\Administrator\.gitconfig
修改全局git用户
git config --global user.name  "username"     
git config --global user.email "email"

本地
Dialer_pre_install\.git\config
修改本地git用户
git config user.name  "username"      
git config user.email "email"

查看
本地
git config user.name

android stuido 
signing:配置可用签名
buildtype：选择签名 debugable true 才能调试
左侧 build variants 选择 run /debug/build版本 run不是可debug版本时候会自动改变buildtype
build的
路径：app\build\outputs\apk\buitest

generate signed （buildtype增加这里对应增加）
路径：Dialer\app\release （可配置）


版本切换
git checkout origin（新建本地分支名） origin/a版本

查看remote 对应的地址 其实他就是个地址 分支概念再它之下
git remote -v

删除origin
git remote rm origin

重新添加origin
git remote add origin git@gitee.com:Hnicolas/Dialer.git 

分支跟踪修改 (local_branch_name可以省略 默认是当前分支)
git  branch -u  origin/remote_branch_name  local_branch_name


查看版本追终
git branch -vv

合并多个commit信息
git rebase -i HEAD~10  10为你要合并的commit个数
insert 把pich 改为 squash 留下一个 系统会处理
esc退出 ：wq 保存
insert 把please之前的都删除 写下你的message
esc 退出 ：wq保存

igone 文件重置

/ 结束的模式只匹配文件夹以及在该文件夹路径下的内容，但是不匹配该文件
/ 开始的模式匹配项目跟目录
如果一个模式不包含斜杠，则它匹配相对于当前 .gitignore 文件路径的内容，如果该模式不在 .gitignore 文件中，则相对于项目根目录

git rm -r --cached .
git add .
git commit -m 'update .gitignore'

manifest 自动添加 设置

但是也有快捷键 把鼠标放在类名上 alt+enter  就有一个add activity to manifest

点击就可以 但是 这玩意如果不小心给弄没了呢

可以这么设置 alt+ctrl+s打开设置

搜索 Intentions 

再在里面搜索 manifest

assets 文件的创建

同样在Project目录结构下：
选择main文件夹右键——》New——》New Directory，文件名为assets就好
或者
右键单击main目录，选择New>Folder>Assets Folder. 注意改路径
